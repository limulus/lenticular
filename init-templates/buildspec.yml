# -------------------------------------------------------------------
# CodeBuild Spec
# -------------------------------------------------------------------

version: 0.2


# -------------------------------------------------------------------
# Environment Variables
# -------------------------------------------------------------------

# env:
#   variables:
#     - FOO: bar


# -------------------------------------------------------------------
# Build Phases
# -------------------------------------------------------------------

phases:

  install:
    commands:
      - npm install --global lenticular

  # pre_build:
  #   commands:
  #    - aws ssm get-parameters --names "npmrc" --with-decryption > .npmrc

  build:
    commands:
      - npm install
      - lenticular generate


# -------------------------------------------------------------------
# Artifacts for S3
# -------------------------------------------------------------------

artifacts:
  base-directory: artifacts
  files:
    - '**/*'
